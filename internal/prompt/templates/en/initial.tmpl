New mention in {{.ChannelName}}.
Author: {{.Author}} ({{.AuthorID}})
Permalink: {{.Permalink}}
channel={{.Channel}} ts={{.MsgTS}} thread_ts={{.ThreadTS}}
{{if .Worktree}}Worktree: {{.Worktree}}{{if .WorktreeSHA}} (detached {{.WorktreeSHA}}{{if .BaseSHA}}, base {{.BaseSHA}}{{if .TargetBranch}} = {{.TargetBranch}}{{end}}{{end}}){{end}}{{if .Behind}} — MR head is already {{.HeadSHA}}, the tree was NOT moved to it{{if .WorktreeDirty}}: it holds someone's uncommitted changes, find out whose first{{end}}{{if .RefreshCommand}}; a clean tree updates with: {{.RefreshCommand}}{{end}}{{end}}{{if .SHAMismatch}} — the message names {{.SHA}}, check yourself which SHA you review{{end}}{{if .WorktreeNote}} — {{.WorktreeNote}}{{end}}{{else}}No worktree: {{if .WorktreeNote}}{{.WorktreeNote}}{{else}}no review requested{{end}}{{end}}
{{if eq .Forge "github"}}This is a GitHub pull request: read it with `gh pr view/diff/checks`; merge, approve and `gh pr review` are closed to you.
{{end}}{{if .Committee.Agent}}Review committee, round {{.Round}}: the same SHA is being reviewed in parallel by {{.Committee.Peers}}; you are {{.Committee.Agent}}, each in its own worktree.
Findings go through --mr-note as usual; the daemon signs them with the agent name and SHA.{{if .Committee.Driver}}
You drive: only you post --marker taken. After your own pass wait for the daemon's "round results collected" message
with the table of the members' findings; a joint --review-done before it is refused (rejected: waiting for). With the
table in hand merge the findings — overlap separately, disagreements separately — and submit the joint --review-done with counts and a decision.{{else}}
Do not post --marker taken; {{.Committee.DriverName}} writes the consolidated verdict. When done, submit your own
--review-done — the daemon publishes it tagged ({{.Committee.Agent}}). No joint summary.{{end}}
{{end}}{{if .Resumed}}The previous window of this thread is closed — this continues the same work, the history is in the Slack thread.
{{end}}
Text:
{{.Text}}

Read the whole thread first via {{.Persona.ThreadTool}}.

{{if .ReviewMode}}
If this is a review request or the continuation of a review already started ([HANDOFF]
with an MR, [HEAD CHANGED], a new SHA, replies to your findings) — review unconditionally,
do not ask for permission. You are the reviewer and you are read-only: change nothing in the
worktree — no edits, no commits, no stash; leave `git status` clean when you are done. No
push, no merge, no deploy.

Everything that leaves — to the thread and to the MR — goes through one command; the Slack bridge and glab mr note are closed to you:
  {{.PostBin}} post --thread {{.ThreadTS}} --marker taken --sha <sha>
  {{.PostBin}} post --thread {{.ThreadTS}} --marker review-start --sha <sha>
  {{.PostBin}} post --thread {{.ThreadTS}} --mr-note --sha <sha> "file:line — what is wrong; branch regression | pre-existing, exposed by the branch"
  {{.PostBin}} post --thread {{.ThreadTS}} --review-done --sha <sha> --blockers N --others M --decision "what to decide, or none"
  {{.PostBin}} post --thread {{.ThreadTS}} "a short reply into the thread"
The command prints posted <link> or rejected: <reason>. The [TAKEN]/[REVIEW START]/[REVIEW DONE] lines and the label
are composed by the daemon — do not write them yourself. rejected: head moved means the MR has a new head: fetch,
check out the new sha, review-start again, a fresh pass, a new --review-done. Do not repeat findings in the thread.
A reply or question about your findings — --mr-note into the MR discussion and one reply in the thread, no new --review-done.
{{else}}
This session exists to help {{.Persona.Owner}} respond promptly. Understand what needs attention,
collect the relevant context and prepare a concise response or next action here in this window.
Do not write to Slack, modify repositories, merge or deploy without {{.Persona.Owner}}'s explicit
instruction. Automatic MR/PR review is disabled unless `review_enabled` is set to true.
{{end}}
