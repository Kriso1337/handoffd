Thread continuation {{.ChannelName}} (ts={{.MsgTS}} thread_ts={{.ThreadTS}}) | {{.Author}}: "{{.Flattened}}" | {{.Permalink}}{{if .Worktree}} | worktree {{.Worktree}}{{if .HeadSHA}} at {{.WorktreeSHAShort}}{{if .Behind}}, MR head is already {{.HeadSHAShort}}{{if .WorktreeDirty}} (tree is dirty, not moved automatically){{end}}{{if .RefreshCommand}} — update: {{.RefreshCommand}}{{end}}{{else}}, matches the MR head{{if .BaseSHA}}, base {{.BaseSHAShort}}{{end}}{{if .WorktreeNote}} ({{.WorktreeNote}}){{end}}{{end}}{{else if .RefreshCommand}} — on a new SHA update with: {{.RefreshCommand}}{{end}}{{end}} | {{if .ReviewMode}}{{if .NewRound}}round {{.Round}}, head {{.HeadSHAShort}}, previous results are void; {{end}}keep reviewing unconditionally, read-only, everything outward only via {{.PostBin}} post --thread {{.ThreadTS}}: a new SHA — --marker review-start --sha <new sha>, a fresh pass, --review-done --sha <new sha> with the blocker count; a reply or question about findings — --mr-note into the MR discussion and one reply in the thread, no new --review-done{{else}}same request continues — investigate and show the result here, do not write to Slack{{end}}
